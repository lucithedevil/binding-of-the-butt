{
  "name": "buttisaac-companion",
  "version": "1.0.0",
  "description": "Local companion for Binding of the Butt – relays game events to Buttplug",
  "main": "companion.js",
  "bin": "companion.js",
  "scripts": {
    "start": "node companion.js",
    "prebuild": "node -e \"require('fs').mkdirSync('out',{recursive:true})\"",
    "build": "pkg companion.js --targets node18-win-x64 --output out/companion.exe && npm run verify:build && npm run stage:release",
    "verify:build": "node scripts/verify-build.js",
    "stage:release": "node scripts/stage-release.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "buttplug": "^3.2.2",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
